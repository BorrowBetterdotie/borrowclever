# Loans & Cards API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Expose `data/loans.json` and `data/cards.json` as a key-authenticated, rate-limited JSON API (`/api/v1/loans`, `/api/v1/cards`) on the existing Cloudflare Worker, with manual key issuance and D1-backed usage metering, per `docs/superpowers/specs/2026-08-12-loans-cards-api-design.md`.

**Architecture:** New routes on the existing `worker/worker.js`. A new build step (`build-api-data.mjs`) bundles `data/loans.json`/`data/cards.json` into `worker/api-data.generated.js` at build time, mirroring the existing `build-products.mjs` → `products.generated.js` pattern. A new hand-written module `worker/api-auth.js` holds key-hashing, authentication, and rate-limit helpers. Two new D1 tables (`api_keys`, `api_usage`) live in a separate, additive-only migration file (`worker/api-schema.sql`) so they can never be touched by `worker/schema.sql`'s destructive "fresh start" `DROP TABLE` pattern.

**Tech Stack:** Cloudflare Workers (vanilla JS, ES modules), Cloudflare D1 (SQLite), Wrangler CLI (via `npx`, not globally installed), Node.js build scripts (no bundler/framework).

## Global Constraints

- Never edit `worker/products.generated.js` or `worker/api-data.generated.js` by hand — both are regenerated by their build script and explicitly marked `DO NOT EDIT BY HAND`.
- Never add a `DROP TABLE` for `api_keys` or `api_usage` to any schema file — those tables hold real customer keys once live.
- API keys are stored only as SHA-256 hashes (`api_keys.key_hash`). The plaintext key is returned exactly once, at issuance, and never persisted or logged.
- Auth is header-only (`Authorization: Bearer <key>`) — never accept a key via query string.
- Two steps in this plan touch **production** state (Cloudflare Worker routes in `wrangler.toml`, the production D1 database, and Worker secrets) and must not be run without the user's explicit go-ahead at that point, per standing instruction. They are marked **STOP — confirm with user** below. Every other task operates on local files or the local `wrangler dev` D1 instance and can proceed without pausing.
- Match existing code style: no framework, no TypeScript, template-string HTML generation, `esc()`-style manual escaping, `ctx.waitUntil()` for fire-and-forget D1 writes.

---

## File Structure

- `data/cards.json` — **modify**: populate the currently-empty `cards` array with the 12 real card records already live in `cards.html`.
- `build-api-data.mjs` — **create**: reads `data/loans.json` + `data/cards.json`, writes `worker/api-data.generated.js`.
- `worker/api-data.generated.js` — **generated** (gitignored, not written by hand).
- `worker/api-schema.sql` — **create**: additive D1 migration for `api_keys` and `api_usage`.
- `worker/api-auth.js` — **create**: key generation/hashing, authentication, usage read/increment, rate-limit timing helpers. No Worker routing logic here — pure functions taking `env`/`request`.
- `worker/worker.js` — **modify**: import the two new modules, add routes for `/api/v1/loans`, `/api/v1/cards`, `/admin/keys` (POST), `/admin/keys/usage` (GET).
- `worker/wrangler.toml` — **modify** (production config — confirm first): add `routes` entries for `/api/*` and `/admin/*`.
- `api-docs.html` — **create**: static docs page at repo root (flat, like `about.html`/`how-we-make-money.html` — *not* nested under `/api/`, since a nested path would break every relative link those templates use).
- `package.json` — **modify**: add `node build-api-data.mjs` to the `build` script.
- `.gitignore` — **modify**: add `worker/api-data.generated.js` and `worker/.dev.vars`.

---

### Task 1: Populate `data/cards.json` with real card data

**Files:**
- Modify: `data/cards.json`

**Interfaces:**
- Produces: `cards.json`'s `cards` array — 12 objects, each with `id, lender, product, icon, iconCls, network, purchaseRate, apr, balanceTransferOffer, annualFee, notes` (and `badgeCheapestOverall: true` on exactly one). `purchaseRate` and `balanceTransferOffer` and `notes` are `string | null`. `apr` and `annualFee` are numbers. This exact shape is consumed by Task 2's `build-api-data.mjs` and re-exposed by Task 6's `/api/v1/cards` route.

This data is transcribed directly from the live `<tr>` rows in `cards.html` (lines 457–769) — every lender, rate, offer, and fee below matches what's already published on the site today.

- [ ] **Step 1: Replace the contents of `data/cards.json`**

```json
{
  "meta": {
    "last_full_review": "2026-07-22"
  },
  "cards": [
    {
      "id": "aib-click",
      "lender": "AIB",
      "product": "Click",
      "icon": "AIB",
      "iconCls": "aib",
      "network": "Visa",
      "purchaseRate": "9.11%",
      "apr": 13.8,
      "balanceTransferOffer": null,
      "annualFee": 0,
      "notes": "Online-only application. No 0% intro — but its 13.8% standard APR is Ireland's lowest, so it can work out cheaper than cards that revert to ~22.9% once a 0% period ends.",
      "badgeCheapestOverall": true
    },
    {
      "id": "anpost-flex",
      "lender": "An Post Money",
      "product": "Flex",
      "icon": "AN POST",
      "iconCls": "anpost",
      "network": "Mastercard",
      "purchaseRate": "10.8%",
      "apr": 15.7,
      "balanceTransferOffer": null,
      "annualFee": 0,
      "notes": "0% on purchases for 9 months (Ireland's longest purchase intro). Money transfers 5.9% for 24 months. Instalment plan from 8.6% APR on purchases over €250."
    },
    {
      "id": "aib-platinum",
      "lender": "AIB",
      "product": "Platinum",
      "icon": "AIB",
      "iconCls": "aib",
      "network": "Visa",
      "purchaseRate": "11.84%",
      "apr": 17.0,
      "balanceTransferOffer": "3.83% (variable) on purchases & balance transfers for first 12 months from account opening, then reverts to standard rate. Balance transfers from store cards or non-AIB cards only; capped at €5,000.",
      "annualFee": 0,
      "notes": "Minimum €40k salary. Cash back 0.5% on annual spend €5k–€50k (max €225/yr). Up to 56 days interest-free if cleared in full."
    },
    {
      "id": "revolut-credit-card",
      "lender": "Revolut",
      "product": "Credit Card",
      "icon": "REVOLUT",
      "iconCls": "revolut",
      "network": "Visa / Mastercard",
      "purchaseRate": null,
      "apr": 17.99,
      "balanceTransferOffer": "0% on balance transfers & purchases for up to 6 months (must meet minimum monthly payments).",
      "annualFee": 0,
      "notes": "App-only, near-instant decision. Limit up to €10,000. RevPoints rewards. Cash-advance APR 19.99%."
    },
    {
      "id": "boi-platinum-advantage",
      "lender": "Bank of Ireland",
      "product": "Platinum Advantage",
      "icon": "BOI",
      "iconCls": "boi",
      "network": "Mastercard",
      "purchaseRate": "13.79%",
      "apr": 19.6,
      "balanceTransferOffer": "0% on balance transfers for 7 months or 0% on purchases for 6 months (new customers choose one), then reverts to standard rate.",
      "annualFee": 76.18,
      "notes": "Worldwide travel insurance. Minimum €40k salary."
    },
    {
      "id": "boi-affinity",
      "lender": "Bank of Ireland",
      "product": "Affinity",
      "icon": "BOI",
      "iconCls": "boi",
      "network": "Mastercard",
      "purchaseRate": "14.57%",
      "apr": 20.2,
      "balanceTransferOffer": "2.9% (fixed) on balance transfers for 12 months. No intro purchase offer. Then reverts to standard rate.",
      "annualFee": 0,
      "notes": "Charity-linked cards."
    },
    {
      "id": "boi-classic",
      "lender": "Bank of Ireland",
      "product": "Classic",
      "icon": "BOI",
      "iconCls": "boi",
      "network": "Mastercard",
      "purchaseRate": "16.12%",
      "apr": 22.1,
      "balanceTransferOffer": "0% on balance transfers for 7 months or 0% on purchases for 6 months (new customers choose one), then reverts to standard rate.",
      "annualFee": 0,
      "notes": "No annual card fee."
    },
    {
      "id": "ptsb-ice",
      "lender": "PTSB",
      "product": "Ice",
      "icon": "PTSB",
      "iconCls": "ptsb",
      "network": "Visa",
      "purchaseRate": "16.50%",
      "apr": 22.53,
      "balanceTransferOffer": "0% on balance transfers for 6 months, plus 0% on purchases for 3 months, then reverts to standard rate. One balance transfer per card.",
      "annualFee": 0,
      "notes": null
    },
    {
      "id": "boi-aer",
      "lender": "Bank of Ireland",
      "product": "Aer Credit Card",
      "icon": "BOI",
      "iconCls": "boi",
      "network": "Mastercard",
      "purchaseRate": "14.00%",
      "apr": 22.7,
      "balanceTransferOffer": "0% on balance transfers for 7 months or 0% on purchases for 6 months (new customers choose one), then reverts to standard rate. Annual fee billed as €6.50/month.",
      "annualFee": 78,
      "notes": "Aer Lingus / AerClub rewards."
    },
    {
      "id": "aib-be",
      "lender": "AIB",
      "product": "'be'",
      "icon": "AIB",
      "iconCls": "aib",
      "network": "Visa / Mastercard",
      "purchaseRate": "14.16%–16.79%",
      "apr": 22.9,
      "balanceTransferOffer": null,
      "annualFee": 0,
      "notes": "Two-tier purchase rate."
    },
    {
      "id": "anpost-classic",
      "lender": "An Post Money",
      "product": "Classic",
      "icon": "AN POST",
      "iconCls": "anpost",
      "network": "Mastercard",
      "purchaseRate": "16.8%",
      "apr": 22.9,
      "balanceTransferOffer": "0% on balance transfers for 12 months (must transfer within 90 days of opening), then reverts to 16.8% / 22.9% APR. Ireland's longest 0% balance-transfer window.",
      "annualFee": 0,
      "notes": null
    },
    {
      "id": "avant-one-card",
      "lender": "Avant Money",
      "product": "One Card",
      "icon": "AVANT",
      "iconCls": "avant",
      "network": "Mastercard",
      "purchaseRate": "16.8%",
      "apr": 22.9,
      "balanceTransferOffer": "0% on balance transfers for 9 months (must transfer within 90 days of opening), then reverts to 16.8% standard rate.",
      "annualFee": 0,
      "notes": "Also 0% on purchases (3 mths) & 0% on money transfers (12 mths). €150 cashback on a €1,000+ balance transfer. One of Ireland's longest 0% BT windows."
    }
  ]
}
```

- [ ] **Step 2: Verify it parses and has 12 unique ids**

Run: `node -e "const d=require('./data/cards.json'); console.log(d.cards.length, new Set(d.cards.map(c=>c.id)).size)"`
Expected: `12 12`

- [ ] **Step 3: Commit**

```bash
git add data/cards.json
git commit -m "Populate data/cards.json with the 12 live credit card records"
```

---

### Task 2: Build step to bundle loans/cards data for the Worker

**Files:**
- Create: `build-api-data.mjs`
- Modify: `package.json:6` (the `"build"` script line)
- Modify: `.gitignore`

**Interfaces:**
- Consumes: `data/loans.json` (`{ meta: { last_full_review }, loans: [...] }`), `data/cards.json` (`{ meta: { last_full_review }, cards: [...] }` — populated in Task 1).
- Produces: `worker/api-data.generated.js` exporting `API_DATA = { loans: { last_full_review, items }, cards: { last_full_review, items } }`. Task 6's `worker.js` imports `API_DATA` by this exact shape.

- [ ] **Step 1: Create `build-api-data.mjs`**

```js
// build-api-data.mjs
// Bundles data/loans.json and data/cards.json into worker/api-data.generated.js
// for the /api/v1/{loans,cards} routes. Run after editing either file:
//   node build-api-data.mjs
// The Worker never reads these JSON files directly — it imports this generated map.

import { readFileSync, writeFileSync } from "node:fs";

const loans = JSON.parse(readFileSync(new URL("./data/loans.json", import.meta.url)));
const cards = JSON.parse(readFileSync(new URL("./data/cards.json", import.meta.url)));

const map = {
  loans: { last_full_review: loans.meta.last_full_review, items: loans.loans },
  cards: { last_full_review: cards.meta.last_full_review, items: cards.cards },
};

const out =
  "// AUTO-GENERATED from data/loans.json and data/cards.json by build-api-data.mjs — DO NOT EDIT BY HAND.\n" +
  "export const API_DATA = " + JSON.stringify(map, null, 2) + ";\n";

writeFileSync(new URL("./worker/api-data.generated.js", import.meta.url), out);

console.log(`[build-api-data] worker/api-data.generated.js: ${map.loans.items.length} loans, ${map.cards.items.length} cards.`);
```

- [ ] **Step 2: Run it and verify output**

Run: `node build-api-data.mjs`
Expected: `[build-api-data] worker/api-data.generated.js: 14 loans, 12 cards.` (loan count depends on current `data/loans.json`; confirm it's non-zero and cards is exactly `12`)

Then: `node -e "import('./worker/api-data.generated.js').then(m => console.log(Object.keys(m.API_DATA), m.API_DATA.cards.items.length))"`
Expected: `[ 'loans', 'cards' ] 12`

- [ ] **Step 3: Add the new generated file to `.gitignore`**

In `.gitignore`, add two lines (matching the existing `worker/products.generated.js` entry):

```
worker/api-data.generated.js
worker/.dev.vars
```

- [ ] **Step 4: Wire the build step into `package.json`**

Change the `"build"` script from:
```json
"build": "node build-products.mjs && node build-table.mjs index.html && node scripts/build-rate-tracker.mjs && node scripts/build-worked-examples.mjs && node scripts/build-calculator.mjs",
```
to:
```json
"build": "node build-products.mjs && node build-api-data.mjs && node build-table.mjs index.html && node scripts/build-rate-tracker.mjs && node scripts/build-worked-examples.mjs && node scripts/build-calculator.mjs",
```

- [ ] **Step 5: Run the full build and confirm it still succeeds**

Run: `npm run build`
Expected: exits 0, prints the existing build-step lines plus the new `[build-api-data] ...` line, no errors. (Note: this repo has had `npm run build` clobber `table.css` before — after this runs, check `git status` and run `git checkout -- table.css` if it shows modified, per existing project convention.)

- [ ] **Step 6: Commit**

```bash
git add build-api-data.mjs package.json .gitignore
git commit -m "Add build-api-data.mjs to bundle loans/cards JSON for the Worker API"
```

---

### Task 3: Additive D1 migration file for API keys and usage

**Files:**
- Create: `worker/api-schema.sql`

**Interfaces:**
- Produces: `api_keys` and `api_usage` table definitions, consumed by Task 4/5/6's SQL statements. This file is **not applied to the production D1 database in this task** — only created. Applying it locally happens in Task 5 (local `wrangler dev` D1); applying it to production is the explicitly-gated step in Task 8.

- [ ] **Step 1: Create `worker/api-schema.sql`**

```sql
-- worker/api-schema.sql — API key & usage tables for /api/v1 and /admin routes.
-- Additive only. Unlike worker/schema.sql's "fresh start" pattern, this file
-- must NEVER contain a DROP TABLE for api_keys — that table holds real
-- customer keys once this is live.

CREATE TABLE IF NOT EXISTS api_keys (
  key_hash    TEXT    PRIMARY KEY,          -- sha256(plaintext key), hex — never store the plaintext key
  key_prefix  TEXT    NOT NULL,              -- first 8 chars after "bc_live_", for display/support lookups only
  email       TEXT    NOT NULL,
  tier        TEXT    NOT NULL DEFAULT 'free',    -- 'free' | 'paid' — descriptive label, not what's enforced
  daily_limit INTEGER NOT NULL DEFAULT 100,        -- the number actually checked on every request
  status      TEXT    NOT NULL DEFAULT 'active',   -- 'active' | 'revoked'
  created_at  TEXT    NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_api_keys_email ON api_keys (email);

CREATE TABLE IF NOT EXISTS api_usage (
  key_hash TEXT    NOT NULL,
  day      TEXT    NOT NULL,                 -- 'YYYY-MM-DD', UTC — same convention as clicks.day
  count    INTEGER NOT NULL DEFAULT 0,
  PRIMARY KEY (key_hash, day)
);
CREATE INDEX IF NOT EXISTS idx_api_usage_day ON api_usage (day);
```

- [ ] **Step 2: Commit**

```bash
git add worker/api-schema.sql
git commit -m "Add additive D1 migration for api_keys/api_usage tables"
```

---

### Task 4: Auth and rate-limit helper module

**Files:**
- Create: `worker/api-auth.js`

**Interfaces:**
- Consumes: `env.DB` (existing D1 binding, same one used for `clicks`).
- Produces (consumed by Task 5 and Task 6's routes in `worker.js`):
  - `generateApiKey(): { plaintext: string, keyPrefix: string }`
  - `hashApiKey(plaintextKey: string): Promise<string>` (hex SHA-256)
  - `authenticateApiKey(request: Request, env): Promise<{ ok: true, keyHash: string, dailyLimit: number } | { ok: false, status: number, body: object }>`
  - `getUsageToday(env, keyHash: string): Promise<number>`
  - `usageIncrementStatement(env, keyHash: string): D1PreparedStatement` (caller must `ctx.waitUntil(...run())`)
  - `secondsUntilNextUTCMidnight(): number`
  - `todayUTC(): string` (`'YYYY-MM-DD'`)

- [ ] **Step 1: Create `worker/api-auth.js`**

```js
// worker/api-auth.js
// Auth + rate-limiting helpers for the /api/v1 and /admin routes.
// Keys are never stored in plaintext — only their SHA-256 hash.
// Pure functions only: no routing logic here (that lives in worker.js).

const KEY_PREFIX = "bc_live_";

function toHex(buffer) {
  return [...new Uint8Array(buffer)].map((b) => b.toString(16).padStart(2, "0")).join("");
}

export async function hashApiKey(plaintextKey) {
  const data = new TextEncoder().encode(plaintextKey);
  const digest = await crypto.subtle.digest("SHA-256", data);
  return toHex(digest);
}

export function generateApiKey() {
  const bytes = crypto.getRandomValues(new Uint8Array(24));
  const random = btoa(String.fromCharCode(...bytes)).replace(/[+/=]/g, "").slice(0, 32);
  return { plaintext: KEY_PREFIX + random, keyPrefix: random.slice(0, 8) };
}

export function todayUTC() {
  return new Date().toISOString().slice(0, 10);
}

export function secondsUntilNextUTCMidnight() {
  const now = new Date();
  const nextMidnight = Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate() + 1);
  return Math.ceil((nextMidnight - now.getTime()) / 1000);
}

// Looks up a presented key. Returns { ok: true, keyHash, dailyLimit } or
// { ok: false, status, body } ready to hand straight to Response.json().
export async function authenticateApiKey(request, env) {
  const auth = request.headers.get("authorization") || "";
  const match = auth.match(/^Bearer\s+(\S+)$/i);
  if (!match) {
    return {
      ok: false,
      status: 401,
      body: { error: { code: "unauthorized", message: "Missing API key. Send it as: Authorization: Bearer <key>" } },
    };
  }

  const keyHash = await hashApiKey(match[1]);
  const row = await env.DB.prepare(
    "SELECT key_hash, daily_limit, status FROM api_keys WHERE key_hash = ?"
  ).bind(keyHash).first();

  if (!row) {
    return { ok: false, status: 401, body: { error: { code: "unauthorized", message: "Unknown API key." } } };
  }
  if (row.status === "revoked") {
    return { ok: false, status: 401, body: { error: { code: "revoked", message: "This API key has been revoked." } } };
  }

  return { ok: true, keyHash: row.key_hash, dailyLimit: row.daily_limit };
}

export async function getUsageToday(env, keyHash) {
  const row = await env.DB.prepare(
    "SELECT count FROM api_usage WHERE key_hash = ? AND day = ?"
  ).bind(keyHash, todayUTC()).first();
  return row ? row.count : 0;
}

// Caller wraps this in ctx.waitUntil(...run()) — same fire-and-forget
// pattern as the existing `clicks` counter in worker.js.
export function usageIncrementStatement(env, keyHash) {
  return env.DB.prepare(
    "INSERT INTO api_usage (key_hash, day, count) VALUES (?, ?, 1) " +
    "ON CONFLICT(key_hash, day) DO UPDATE SET count = count + 1"
  ).bind(keyHash, todayUTC());
}
```

- [ ] **Step 2: Sanity-check the hashing is deterministic**

Run:
```bash
node -e "
import('./worker/api-auth.js').then(async (m) => {
  const a = await m.hashApiKey('bc_live_test123');
  const b = await m.hashApiKey('bc_live_test123');
  const c = await m.hashApiKey('bc_live_different');
  console.log(a === b, a !== c, a.length);
});
"
```
Expected: `true true 64`

- [ ] **Step 3: Commit**

```bash
git add worker/api-auth.js
git commit -m "Add auth/rate-limit helper module for the API routes"
```

---

### Task 5: Admin key-issuance routes

**Files:**
- Modify: `worker/worker.js` (imports at top; two new handler functions above `export default`; two new route branches inside `fetch`)
- Create: `worker/.dev.vars` (local-only, gitignored by Task 2)

**Interfaces:**
- Consumes: `generateApiKey`, `hashApiKey` from `worker/api-auth.js` (Task 4). `env.ADMIN_TOKEN` (new secret, set locally via `.dev.vars` for this task; production secret is set in Task 8).
- Produces: `POST /admin/keys` and `GET /admin/keys/usage` routes. Task 6 tests will mint a real key through this route rather than hand-inserting D1 rows.

- [ ] **Step 1: Add the new imports to the top of `worker/worker.js`**, right after the existing `import { PRODUCTS } from "./products.generated.js";` line:

```js
import { generateApiKey, hashApiKey, authenticateApiKey, getUsageToday, usageIncrementStatement, secondsUntilNextUTCMidnight } from "./api-auth.js";
```

- [ ] **Step 2: Add `handleIssueKey` and `handleKeyUsage`**, placed directly above the `export default {` line in `worker/worker.js`:

```js
async function handleIssueKey(request, env) {
  const token = request.headers.get("x-admin-token") || "";
  if (!env.ADMIN_TOKEN || token !== env.ADMIN_TOKEN) {
    return new Response("Unauthorized", { status: 401 });
  }

  let payload;
  try {
    payload = await request.json();
  } catch {
    return Response.json({ error: { code: "bad_request", message: "Body must be JSON." } }, { status: 400 });
  }

  const email = String(payload.email || "").trim();
  const tier = payload.tier === "paid" ? "paid" : "free";
  if (!email) {
    return Response.json({ error: { code: "bad_request", message: "email is required." } }, { status: 400 });
  }

  const dailyLimit = tier === "paid" ? 10000 : 100;
  const { plaintext, keyPrefix } = generateApiKey();
  const keyHash = await hashApiKey(plaintext);

  await env.DB.prepare(
    "INSERT INTO api_keys (key_hash, key_prefix, email, tier, daily_limit, status, created_at) VALUES (?, ?, ?, ?, ?, 'active', ?)"
  ).bind(keyHash, keyPrefix, email, tier, dailyLimit, new Date().toISOString()).run();

  return Response.json({ key: plaintext, key_prefix: keyPrefix, tier, daily_limit: dailyLimit });
}

async function handleKeyUsage(request, env) {
  const url = new URL(request.url);
  const token = url.searchParams.get("token") || request.headers.get("x-admin-token") || "";
  if (!env.ADMIN_TOKEN || token !== env.ADMIN_TOKEN) {
    return new Response("Unauthorized", { status: 401 });
  }

  const rows = await env.DB.prepare(
    "SELECT k.key_prefix, k.email, k.tier, k.daily_limit, k.status, " +
    "SUM(CASE WHEN u.day >= date('now','start of month') THEN u.count ELSE 0 END) AS this_month, " +
    "SUM(u.count) AS all_time " +
    "FROM api_keys k LEFT JOIN api_usage u ON u.key_hash = k.key_hash " +
    "GROUP BY k.key_hash ORDER BY k.created_at DESC"
  ).all();

  return Response.json({ generated: new Date().toISOString(), keys: rows.results });
}
```

- [ ] **Step 3: Add the two routes inside `fetch`**, in `worker/worker.js`, right after the existing `/stats` branch (before the `/go/` regex match):

```js
    if (url.pathname === "/admin/keys" && request.method === "POST") {
      return handleIssueKey(request, env);
    }

    if (url.pathname === "/admin/keys/usage") {
      return handleKeyUsage(request, env);
    }
```

- [ ] **Step 4: Create local dev secrets and local D1 tables** (local only — does not touch production)

Create `worker/.dev.vars`:
```
ADMIN_TOKEN=local-dev-admin-token
```

Apply both schema files to the **local** D1 instance (the `--local` flag is required — without it this would target production):
```bash
cd worker
npx wrangler d1 execute borrowclever-clicks --local --file=schema.sql
npx wrangler d1 execute borrowclever-clicks --local --file=api-schema.sql
```
Expected: both commands report success creating the tables (a fresh local `.wrangler/state` SQLite file).

- [ ] **Step 5: Start `wrangler dev` and test key issuance**

```bash
cd worker
npx wrangler dev
```
In another terminal, with the dev server running (default `http://localhost:8787`):
```bash
curl -s -X POST http://localhost:8787/admin/keys \
  -H "X-Admin-Token: local-dev-admin-token" \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","tier":"free"}'
```
Expected: `200` with JSON like `{"key":"bc_live_...","key_prefix":"........","tier":"free","daily_limit":100}` — **save this `key` value**, it's used in Task 6.

Also verify the auth gate and usage listing:
```bash
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://localhost:8787/admin/keys -d '{}'
curl -s http://localhost:8787/admin/keys/usage -H "X-Admin-Token: local-dev-admin-token"
```
Expected: first prints `401`; second prints JSON with a `keys` array containing the key just issued.

- [ ] **Step 6: Commit**

```bash
git add worker/worker.js
git commit -m "Add admin key-issuance and usage routes"
```
(`worker/.dev.vars` is gitignored — do not add it.)

---

### Task 6: `/api/v1/loans` and `/api/v1/cards` routes

**Files:**
- Modify: `worker/worker.js`

**Interfaces:**
- Consumes: `API_DATA` from `worker/api-data.generated.js` (Task 2), `authenticateApiKey`, `getUsageToday`, `usageIncrementStatement`, `secondsUntilNextUTCMidnight` from `worker/api-auth.js` (Task 4).
- Produces: the two public API routes, matching the response envelope from the design spec §4 and error shapes from §7.

- [ ] **Step 1: Add the `API_DATA` import** to the top of `worker/worker.js`, alongside the other imports:

```js
import { API_DATA } from "./api-data.generated.js";
```

- [ ] **Step 2: Add `handleApi`**, placed above `export default {` (near the other handlers added in Task 5):

```js
async function handleApi(request, env, ctx, resource) {
  const auth = await authenticateApiKey(request, env);
  if (!auth.ok) {
    return Response.json(auth.body, { status: auth.status });
  }

  const usedToday = await getUsageToday(env, auth.keyHash);
  if (usedToday >= auth.dailyLimit) {
    const retryAfter = secondsUntilNextUTCMidnight();
    return Response.json(
      {
        error: {
          code: "rate_limited",
          message: `Daily limit of ${auth.dailyLimit} requests reached.`,
          reset: new Date(Date.now() + retryAfter * 1000).toISOString(),
        },
      },
      { status: 429, headers: { "Retry-After": String(retryAfter) } }
    );
  }

  ctx.waitUntil(
    usageIncrementStatement(env, auth.keyHash).run()
      .catch((e) => console.error("api usage count failed:", e))
  );

  const source = API_DATA[resource];
  const body = {
    meta: {
      generated: new Date().toISOString(),
      last_full_review: source.last_full_review,
      source: `https://borrowclever.ie/${resource}.html`,
    },
    data: source.items,
  };

  return Response.json(body, { headers: { "Cache-Control": "private, no-store" } });
}
```

- [ ] **Step 3: Add the two routes inside `fetch`**, right after the `/admin/keys/usage` branch added in Task 5:

```js
    if (url.pathname === "/api/v1/loans") {
      return handleApi(request, env, ctx, "loans");
    }

    if (url.pathname === "/api/v1/cards") {
      return handleApi(request, env, ctx, "cards");
    }
```

- [ ] **Step 4: Restart `wrangler dev` (to pick up the new generated import) and test end-to-end**

```bash
cd worker
npx wrangler dev
```

Using the `bc_live_...` key saved from Task 5, Step 5:
```bash
KEY="bc_live_REPLACE_WITH_YOUR_KEY"

# No key
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8787/api/v1/loans
# Bad key
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8787/api/v1/loans -H "Authorization: Bearer bc_live_wrong"
# Valid key
curl -s http://localhost:8787/api/v1/loans -H "Authorization: Bearer $KEY" | head -c 300; echo
curl -s http://localhost:8787/api/v1/cards -H "Authorization: Bearer $KEY" | head -c 300; echo
```
Expected: `401`, then `401`, then two `200` JSON bodies each starting `{"meta":{"generated":"...","last_full_review":"2026-07-22","source":"https://borrowclever.ie/loans.html"},"data":[...`. Cards response has 12 items in `data`.

- [ ] **Step 5: Verify rate limiting** (free tier is 100/day — script past it)

```bash
for i in $(seq 1 101); do
  curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8787/api/v1/loans -H "Authorization: Bearer $KEY"
done | tail -5
```
Expected: the last of the 101 requests returns `429` (the prior 100 return `200`). Confirm the `Retry-After` header is present:
```bash
curl -sD - -o /dev/null http://localhost:8787/api/v1/loans -H "Authorization: Bearer $KEY" | grep -i retry-after
```

- [ ] **Step 6: Verify a revoked key is rejected**

```bash
cd worker
npx wrangler d1 execute borrowclever-clicks --local --command "UPDATE api_keys SET status='revoked' WHERE key_prefix='<first 8 chars of your key_prefix from Task 5>'"
curl -s http://localhost:8787/api/v1/loans -H "Authorization: Bearer $KEY"
```
Expected: `401` with `{"error":{"code":"revoked",...}}`.

- [ ] **Step 7: Commit**

```bash
git add worker/worker.js
git commit -m "Add /api/v1/loans and /api/v1/cards routes with key auth and daily rate limiting"
```

---

### Task 7: API docs page

**Files:**
- Create: `api-docs.html` (repo root — flat, not nested, so its relative links to `site.css`/`index.html`/etc. resolve the same way `about.html`'s do)

**Interfaces:**
- No code interfaces — static content page. Links to `/api/v1/loans`, `/api/v1/cards`, and describes the `Authorization: Bearer` header and error codes documented in Task 4/6.

- [ ] **Step 1: Create `api-docs.html`**, following the exact structure of `how-we-make-money.html` (same nav, mobile-nav, footer, meta/OG boilerplate) with API-specific content:

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>API Docs | BorrowClever</title>
<meta name="description" content="BorrowClever Loans & Cards API: authenticated JSON access to Ireland's independent loan and credit card rate comparisons.">
<meta name="robots" content="index, follow">
<link rel="canonical" href="https://borrowclever.ie/api-docs.html">
<meta property="og:type" content="website">
<meta property="og:site_name" content="BorrowClever">
<meta property="og:url" content="https://borrowclever.ie/api-docs.html">
<meta property="og:title" content="API Docs | BorrowClever">
<meta property="og:description" content="BorrowClever Loans & Cards API: authenticated JSON access to Ireland's independent loan and credit card rate comparisons.">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="API Docs | BorrowClever">
<meta name="twitter:description" content="BorrowClever Loans & Cards API: authenticated JSON access to Ireland's independent loan and credit card rate comparisons.">
<link rel="stylesheet" href="site.css">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
<style>
.page-header { padding: 4rem 6%; background: #0c0c0c; }
.page-header-inner { max-width: 860px; }
.page-header h1 { font-size: clamp(1.8rem, 3vw, 2.6rem); margin-bottom: 0.75rem; }
.page-header-sub { font-size: 0.95rem; color: rgba(255,255,255,0.6); line-height: 1.65; margin-bottom: 0; }
.eyebrow { display: inline-block; font-size: 0.72rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: #4ade80; margin-bottom: 1rem; }
.content { max-width: 860px; margin: 0 auto; padding: 4rem 6%; }
.content h2 { font-size: 1.15rem; font-weight: 700; color: #f0f0f0; letter-spacing: -0.02em; margin-top: 2.5rem; margin-bottom: 0.75rem; }
.content h2:first-child { margin-top: 0; }
.content p { font-size: 0.92rem; color: rgba(255,255,255,0.88); line-height: 1.75; margin-bottom: 1rem; }
.content ul { margin: 0.5rem 0 1rem 1.25rem; display: flex; flex-direction: column; gap: 0.4rem; }
.content ul li { font-size: 0.92rem; color: rgba(255,255,255,0.88); line-height: 1.65; }
.content a { color: #22c55e; }
.content code { background: rgba(255,255,255,0.08); padding: 0.15rem 0.4rem; border-radius: 4px; font-size: 0.85em; }
.content pre { background: #141414; border: 1px solid rgba(255,255,255,0.08); border-radius: 10px; padding: 1rem 1.25rem; overflow-x: auto; margin: 1rem 0; }
.content pre code { background: none; padding: 0; }
.info-box { background: rgba(34,197,94,0.06); border: 1px solid rgba(34,197,94,0.2); border-radius: 12px; padding: 1.25rem 1.5rem; margin: 2rem 0; }
.info-box p { font-size: 0.88rem; color: #f0f0f0; margin: 0; font-weight: 500; }
.divider { border: none; border-top: 1px solid rgba(255,255,255,0.08); margin: 2rem 0; }
@media (max-width: 900px) { .content { padding: 3rem 5%; } }
</style>
</head>
<body>

<nav>
  <a href="index.html" class="nav-logo">Borrow<span>Clever</span></a>
  <ul class="nav-links">
    <li><a href="loans.html">Compare loans</a></li>
    <li><a href="cards.html">Compare cards</a></li>
    <li><a href="/rate-tracker/">Rate tracker</a></li>
    <li><a href="/calculator/">Calculator</a></li>
    <li><a href="index.html#signup" class="nav-cta">Newsletter</a></li>
  </ul>
  <button class="nav-hamburger" id="hamburger" aria-label="Open menu">
    <span></span><span></span><span></span>
  </button>
</nav>

<div class="mobile-nav" id="mobile-nav">
  <a href="loans.html">Compare loans</a>
  <a href="cards.html">Compare cards</a>
  <a href="/rate-tracker/">Rate tracker</a>
  <a href="/calculator/">Calculator</a>
  <a href="index.html#signup" class="mobile-cta">Newsletter</a>
</div>

<div class="page-header">
  <div class="page-header-inner">
    <div class="eyebrow">Developers</div>
    <h1>BorrowClever API</h1>
    <p class="page-header-sub">Authenticated JSON access to the same loan and credit card comparison data published on this site.</p>
  </div>
</div>

<div class="content">

  <h2>Base URL and authentication</h2>
  <p>All requests require an API key, sent as a bearer token:</p>
  <pre><code>Authorization: Bearer bc_live_your_key_here</code></pre>
  <p>Keys are issued manually. Email <a href="mailto:hello@borrowclever.ie">hello@borrowclever.ie</a> to request one.</p>

  <hr class="divider">

  <h2>Endpoints</h2>
  <p><code>GET https://borrowclever.ie/api/v1/loans</code> — every personal loan currently on the <a href="loans.html">loans comparison page</a>.</p>
  <p><code>GET https://borrowclever.ie/api/v1/cards</code> — every credit card currently on the <a href="cards.html">cards comparison page</a>.</p>
  <p>Example response:</p>
  <pre><code>{
  "meta": {
    "generated": "2026-08-12T10:00:00.000Z",
    "last_full_review": "2026-07-22",
    "source": "https://borrowclever.ie/loans.html"
  },
  "data": [
    { "id": "boi-sbci-green", "lender": "Bank of Ireland", "apr": 2.95, "...": "..." }
  ]
}</code></pre>

  <hr class="divider">

  <h2>Rate limits</h2>
  <p>Limits are per key, per UTC day:</p>
  <ul>
    <li><strong>Free</strong> — 100 requests/day</li>
    <li><strong>Paid</strong> — 10,000 requests/day</li>
  </ul>
  <p>Exceeding your limit returns <code>429</code> with a <code>Retry-After</code> header (seconds until the next UTC midnight).</p>

  <hr class="divider">

  <h2>Errors</h2>
  <p>Errors are returned as <code>{ "error": { "code": "...", "message": "..." } }</code>:</p>
  <ul>
    <li><code>401 unauthorized</code> — missing or unknown key</li>
    <li><code>401 revoked</code> — key has been revoked</li>
    <li><code>429 rate_limited</code> — daily limit reached</li>
  </ul>

  <div class="info-box">
    <p>This API is separate from BorrowClever's comparison tables — the ranking methodology described on <a href="how-we-make-money.html">how we make money</a> applies to the site, not to API access itself.</p>
  </div>

</div>

<footer>
  <a href="index.html" class="footer-logo">Borrow<span>Clever</span></a>
  <div class="footer-links">
    <a href="about.html">About</a>
    <a href="how-we-make-money.html">How we make money</a>
    <a href="privacy.html">Privacy Policy</a>
  </div>
  <div class="footer-note">BorrowClever is an independent financial comparison service. Rates sourced from lender websites and CCPC.ie. Always verify with the lender before applying. This is not financial advice. © 2026 BorrowClever Ireland Limited.</div>
</footer>

<script>
const hamburger = document.getElementById('hamburger');
const mobileNav = document.getElementById('mobile-nav');
hamburger.addEventListener('click', () => {
  const open = mobileNav.classList.toggle('open');
  hamburger.classList.toggle('open', open);
  hamburger.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
});
mobileNav.querySelectorAll('a').forEach(a => {
  a.addEventListener('click', () => {
    mobileNav.classList.remove('open');
    hamburger.classList.remove('open');
  });
});
</script>

</body>
</html>
```

- [ ] **Step 2: Open it locally and sanity-check rendering**

Run: `python3 -m http.server 8080` from the repo root, then open `http://localhost:8080/api-docs.html` in a browser (or use the project's own browser-preview workflow). Confirm: nav/footer match `how-we-make-money.html`'s look, code blocks are legible, mobile hamburger menu opens/closes.

- [ ] **Step 3: Commit**

```bash
git add api-docs.html
git commit -m "Add API docs page at /api-docs.html"
```

---

### Task 8: Production deployment — STOP, confirm with user before running

**Files:**
- Modify: `worker/wrangler.toml` (production Worker routing — **do not edit or apply without explicit confirmation**)
- Production D1 database (`borrowclever-clicks`) — **do not run the migration without explicit confirmation**
- Production Worker secret `ADMIN_TOKEN` — **do not set without explicit confirmation**

Everything in Tasks 1–7 is local/file-only and safe to complete unattended. This task is the one place the plan touches shared, hard-to-reverse production state, matching the standing instruction to ask before touching the Worker's production config or running a D1 migration. Do not execute the steps below without asking the user first, immediately before doing so — not just once at plan approval time.

- [ ] **Step 1 (after confirmation): Add API/admin routes to `worker/wrangler.toml`**

In the `routes = [...]` array, add two lines after the existing `/stats*` entry:
```toml
  { pattern = "borrowclever.ie/api/*", zone_name = "borrowclever.ie" },
  { pattern = "borrowclever.ie/admin/*", zone_name = "borrowclever.ie" },
```

- [ ] **Step 2 (after confirmation): Apply `worker/api-schema.sql` to production D1**

```bash
cd worker
npx wrangler d1 execute borrowclever-clicks --remote --file=api-schema.sql
```

- [ ] **Step 3 (after confirmation): Set the production `ADMIN_TOKEN` secret**

```bash
cd worker
npx wrangler secret put ADMIN_TOKEN
```
(Enter a strong random value when prompted — not the `local-dev-admin-token` value used in Task 5.)

- [ ] **Step 4 (after confirmation): Deploy**

```bash
npm run deploy
```
(Runs the full `build` script from Task 2, then `wrangler deploy` from the `worker/` directory.)

- [ ] **Step 5: Verify against production**

```bash
curl -s -X POST https://borrowclever.ie/admin/keys \
  -H "X-Admin-Token: <the secret set in Step 3>" \
  -H "Content-Type: application/json" \
  -d '{"email":"yourself@example.com","tier":"free"}'
```
Then repeat the `curl` checks from Task 6 Step 4 against `https://borrowclever.ie/api/v1/loans` and `/api/v1/cards` with the returned key.

- [ ] **Step 6: Commit the wrangler.toml change**

```bash
git add worker/wrangler.toml
git commit -m "Route /api/* and /admin/* to the Worker in production"
```
